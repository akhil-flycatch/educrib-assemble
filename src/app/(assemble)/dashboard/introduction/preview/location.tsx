import LocationMap from './components/map/location';

export default function Location({ location, title }) {
    return (
        // location && <LocationMap location={location} title={title} />
        <div></div>
    )
}
